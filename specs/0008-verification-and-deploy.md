# 0008 — 本地驗證與部署

## 本地驗證（bun dev + 本地 D1，全部通過）

| # | 測試 | 預期 | 結果 |
|---|---|---|---|
| 1 | 公開申請 `POST /api/auth/apply` | 201 建立 pending | ✅ |
| 2 | 超級管理員登入（.env） | 200 | ✅ |
| 3 | 超級管理員 `GET /api/users` | 看到 pending 申請者 | ✅ |
| 4 | 重複帳號申請 | 409 | ✅ |
| 5 | 審核通過＋授權 `calendar` | 200 | ✅ |
| 6 | 匿名 `GET /api/users` | 403 | ✅ |
| 7 | 已通過使用者登入 | 200 | ✅ |
| 8 | 該使用者建立課程（有 calendar） | 201 | ✅ |
| 9 | 該使用者建立器材（無 equipment） | 403 | ✅ |
| 10 | 匿名建立課程 | 401 | ✅ |
| 11 | 錯誤密碼登入 | 401 | ✅ |
| 12 | 超級管理員收回 calendar 權限 | 200 | ✅ |
| 13 | 同一舊 session 再建立課程 | **403（即時生效）** | ✅ |
| 14 | 停用帳號 | 200 | ✅ |
| 15 | 被停用者登入 | 403 | ✅ |
| 16 | 超級管理員建立器材（全權限） | 201 | ✅ |
| 17 | `GET /apply`（匿名） | 200 渲染 | ✅ |
| 18 | `GET /admin`（匿名） | 302 導回 `/` | ✅ |
| 19 | `GET /admin`（超級管理員） | 200 | ✅ |

重點：權限後端每請求查 DB，故收回權限/停用**即時生效**（#13、#15）。

`typecheck` / `lint`：本功能新增檔案無新錯誤（既存 5 個型別錯誤與 max-attributes 警告為先前既有）。

## 部署步驟（待執行）

1. 遠端 D1 套用 migration：`bun run db:migrate:remote`
2. 建置並部署：`bun run deploy`
3. 確認生產環境超級管理員帳號＝ Pages secret `NUXT_ADMIN_USERNAME` / `NUXT_ADMIN_PASSWORD`
   （目前為 `tim861125@gmail.com`）。

> 部署為對外、不易復原的動作，待確認後執行。
